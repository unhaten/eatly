export enum AppRoutes {
    HOME = "home",
    
}


export const paths = {
    home: "/",
    register: "/register",
    auth: "auth",
    orders: "/orders",
    messages: "/messages",
    notFound: "*",
};
