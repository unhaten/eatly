import CardOption from '../../shared/ui/CardOption'
import CardReceiptIcon from '../../shared/ui/icons/CardReceiptIcon'

const Invoicing = () => {
	return (
		<>
			<CardOption text={'Invoicing'} icon={<CardReceiptIcon />} />
		</>
	)
}

export default Invoicing
